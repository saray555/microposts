<h3><%= @title %></h3>

<div class="row">

  <% @users.each do |user| %>
    <section class="user_info">
  <ul>
      <h4>
        <%= gravatar_for @user %>
        <%= link_to user.name, user %>
        
        <%#= pluralize(user.microposts.count, "micropost" %>
      </h4>
  </ul>
  </section>
  <% end %>

</div>